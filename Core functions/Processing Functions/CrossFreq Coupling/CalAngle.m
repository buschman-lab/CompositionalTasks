function [RadAng,DegAng] = CalAngle(inputArg1)
%CALANGLE Calculates angle in radian and degree
% RadAng = atan(imag(inputArg1)./real(inputArg1));
% DegAng = atand(imag(inputArg1)./real(inputArg1));

end

